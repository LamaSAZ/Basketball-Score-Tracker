let homeID = document.getElementById("home")
let guestID = document.getElementById("guest")

function addHome1(){
  homeID.textContent = Number(homeID.textContent) + 1
  
}
function addHome2(){
  homeID.textContent = Number(homeID.textContent) + 2
}
function addHome3(){
  homeID.textContent = Number(homeID.textContent) + 3
}

function addGuest1(){
  guestID.textContent = Number(guestID.textContent) + 1
}
function addGuest2(){
  guestID.textContent = Number(guestID.textContent) + 2
}
function addGuest3(){
  guestID.textContent = Number(guestID.textContent) + 3
}